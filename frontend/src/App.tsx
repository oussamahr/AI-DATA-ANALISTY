// Deprecated - new routing lives in app/router.tsx
// Kept for backwards compatibility; main.tsx no longer uses this.
import { AppRouter } from "./app/router";
export function App() {
  return <AppRouter />;
}
